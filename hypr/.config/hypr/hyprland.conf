# vim: ft=hyprlang
# monitor= preferred, 1920x1080@144, 0x0, 1
monitor=,highrr,auto,1

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
exec-once = discord
exec-once = if [ -e $HOME/.dotfiles/options/.laptop ]; then hypridle -c ${XDG_CONFIG_HOME}/hypr/hypridle_laptop.conf; else hypridle; fi
# exec-once = waybar
exec-once = qs -c noctalia-shell
exec-once = awww-daemon
exec-once = emacs --daemon
exec-once = kdeconnect-indicator
exec-once = nm-applet
exec-once = dunst
exec-once = udiskie --smart-tray --file-manager=thunar
# On display manager, select Hyprland (uwsm) session for this to work correctly
exec-once = systemctl --user enable --now hyprpolkitagent.service
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# Clipboard
exec-once = wl-paste --type text --watch cliphist store # Stores only text data
exec-once = wl-paste --type image --watch cliphist store # Stores only image data

# Scratchpad
exec-once = hyprctl dispatch exec "[workspace special silent] foot --title=nvim-scratch nvim"

# Start recording with noctalia-shell's screen recorder plugin (replay buffer)
# Start it after 1 minute to avoid recording the entire startup process, and to ensure it starts after the plugin is loaded
exec-once = sleep 60 && qs -c noctalia-shell ipc call plugin:screen-recorder startReplay

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = si, ru
    kb_variant = ,phonetic
    kb_options = grp:alt_space_toggle

    follow_mouse = 2

    touchpad {
        natural_scroll = yes
        scroll_factor = 0.3
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    gaps_in = 5
    gaps_out = 12
    border_size = -1
    col.active_border = rgba(00000000)
    col.inactive_border = rgba(00000000)

    layout = dwindle
}

dwindle {
    force_split = 2
}

decoration {
    rounding = 10
}

animations {
    enabled = yes
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    pseudotile = yes # Enabling is bound to mainMod + P
    preserve_split = yes # you probably want this
    precise_mouse_move = yes
}

master {
    new_status = master
}

workspace = 1, persistent:true
workspace = 2, persistent:true
workspace = 3, persistent:true
workspace = 4, persistent:true
workspace = 5, persistent:true
workspace = 6, persistent:true
workspace = 7, persistent:true
workspace = 8, persistent:true
workspace = 9, persistent:true
workspace = 10, persistent:true

# Environment variables
env = GTK_THEME, gruvbox-dark-gtk
env = QT_QPA_PLATFORM, wayland;xcb
env = XDG_CURRENT_DESKTOP, Hyprland
env = XDG_SESSION_TYPE, wayland
env = XDG_SESSION_DESKTOP, Hyprland

# Variables
$mainMod = SUPER
$term = foot
$browser = librewolf
$browser2 = chromium
$filemanager = thunar

# Main keybindings
bind = $mainMod, Return, exec, $term
bind = $mainMod, Q, killactive,
bind = $mainMod SHIFT, E, exit,
bind = $mainMod, F, exec, $filemanager
bind = $mainMod, E, exec, emacsclient -c -a 'emacs'
bind = $mainMod, TAB, togglefloating,
bind = $mainMod, D, exec, rofi -show run -show-icons
bind = $mainMod, B, exec, $browser
bind = $mainMod SHIFT, B, exec, $browser2
bind = $mainMod, L, exec, hyprlock
bind = $mainMod SHIFT, L, exec, sh -c "~/.dotfiles/rofi/.config/rofi/scripts/rofi-hyprsunset"
bind = $mainMod, P, exec, sh -c "~/.dotfiles/scripts/better_random_wall.sh random"
bind = $mainMod SHIFT, P, exec, sh -c "~/.dotfiles/scripts/better_random_wall.sh default"
bind = $mainMod, S, exec, sh -c "~/.dotfiles/scripts/spongebob_case.sh"
bind = $mainMod, space, fullscreen
bind = $mainMod, K, exec, rofi -show p -modi "p:~/.config/rofi/scripts/rofi-power-menu --choices=shutdown/reboot/suspend/logout"
bind = , Print, exec, grim -g "$(slurp)" -t ppm - | satty --filename - --fullscreen --output-filename ~/Pictures/Screenshots/satty-$(date '+%Y%m%d-%H:%M:%S').png
bind = $mainMod SHIFT, W, exec, pkill waybar && waybar
bind = $mainMod SHIFT, S, togglespecialworkspace
bind = $mainMod, V, exec, cliphist list | rofi -dmenu | cliphist decode | wl-copy
bind = $mainMod, R, exec, qs -c noctalia-shell ipc call plugin:screen-recorder toggle
bind = $mainMod SHIFT, R, exec, qs -c noctalia-shell ipc call plugin:screen-recorder saveReplay

# Volume control and other media keys
binde= , XF86AudioLowerVolume,  exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 1%-
binde= , XF86AudioRaiseVolume,  exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 1%+
bind = , XF86AudioMute,         exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bind = , XF86AudioMicMute,      exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
binde= , XF86MonBrightnessUp,   exec, brightnessctl s 10%+
binde= , XF86MonBrightnessDown, exec, brightnessctl s 10%-
bind = , XF86AudioPlay,         exec, playerctl play-pause
bind = , XF86AudioPrev,         exec, playerctl previous
bind = , XF86AudioNext,         exec, playerctl next

# Color picker (xcolor)
bind = CTRL, Print, exec, hyprpicker -a

# Move focus with mainMod + arrow keys
bind = ALT, TAB, cyclenext
bind = ALT SHIFT, Tab, cyclenext, prev

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up,   workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Move/resize windows with keyboard
bind = $mainMod SHIFT, left,  movewindow, l
bind = $mainMod SHIFT, right, movewindow, r
bind = $mainMod SHIFT, up,    movewindow, u
bind = $mainMod SHIFT, down,  movewindow, d

binde = $mainMod, right, resizeactive, 10 0
binde = $mainMod, left,  resizeactive, -10 0
binde = $mainMod, up,    resizeactive, 0 -10
binde = $mainMod, down,  resizeactive, 0 10

# Window rules
# Don't lock or go to sleep if fullscreen
windowrule = idle_inhibit fullscreen, match:class .*

# Picture in Picture
windowrule = float on, no_initial_focus on, size 324 181, move ((monitor_w*1)-329) ((monitor_h*1)-212), pin on, animation slide right, match:initial_title .*(?i)picture.in.picture.*

# Sharing indicators for sharing screen in browsers
windowrule = float on, no_initial_focus on, size 324 181, move ((monitor_w*1)-329) ((monitor_h*1)-212), pin on, animation slide right, match:initial_title ^\([^()]+ — [Ss]haring [Ii]ndicator\)$

# Flameshot (screenshots)
windowrule = float on, move (0) (0), suppress_event fullscreen, match:title ^(flameshot)

# File-roller
windowrule = float on, center on, match:class ^(file-roller)$

# Common modals
windowrule = float on, match:title ^(Open)$
windowrule = float on, match:title ^(Choose Files)$
windowrule = float on, match:title ^(Save As)$
windowrule = float on, match:title ^(Confirm to replace files)$
windowrule = float on, match:title ^(File Operation Progress)$
windowrule = float on, match:class ^(google-chrome)$, match:title ^(Open Files)$
windowrule = float on, match:class ^(google-chrome)$, match:title ^(Open File)$

# Scratchpad
windowrule = workspace special, float on, size 1280 720, match:title ^(nvim-scratch)$

# Swaync
layerrule = blur on, ignore_alpha 0.5, match:namespace swaync-control-center
layerrule = blur on, ignore_alpha 0.5, match:namespace swaync-notification-window
